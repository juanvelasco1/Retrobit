// eventsExampleHandlers.js

const { StartTriviaHandler } = require("../utils/helpers");

// Assuming db and io are required or passed in some way to be accessible
const StartTriviaHandler = (socket, db, io) => {
  return () => {};
};



module.exports = {
    StartTriviaHandler,
  
};
