// eventsExampleHandlers.js

const { CodeQrHandler } = require("../utils/helpers");

// Assuming db and io are required or passed in some way to be accessible
const CodeQrHandler = (socket, db, io) => {
  return () => {};
};



module.exports = {
    CodeQrHandler,
  
};
